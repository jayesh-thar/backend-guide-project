Yes solid progress today for a first session:

✅ Full auth flow diagram — multiple iterations, each one better
✅ Understood JWT + Cookie concept
✅ ES Modules vs CommonJS — import/export
✅ dotenv setup and why it exists
✅ Basic Express server running


Tomorrow We Continue With:

MongoDB Atlas setup + connection (config/db.js)
User model / schema (models/user.model.js)
Then slowly into routes and controllers


----
Day 02:

*Step 1 → Connect MongoDB          (config/db.js)
            ↓
*Step 2 → Update server.js         (call db connection)
            ↓
*Step 3 → Create User Model        (models/user.model.js)
            ↓
Step 4 → Setup Routes             (routes/auth.routes.js)
            ↓
Step 5 → Write Controllers        (controllers/auth.controller.js)
            ↓
Step 6 → Test with Postman        (register/login/logout)
            ↓
Step 7 → Add Email                (mailtrap setup)
            ↓
Step 8 → Add Middleware           (protect routes)

Code Written ✅

src/config/db.js — MongoDB connection with error handling
src/models/user.model.js — User schema with all fields, timestamps
server.js — updated with DB connection call

Concepts Understood ✅

.then() and .catch() — success and failure handling
Schema fields — types, required, unique, trim, lowercase
new keyword for classes like mongoose.Schema
{ timestamps: true } — auto createdAt and updatedAt
mongoose.model('Name', schema) — creating a model
HTTP methods — GET, POST, PUT, DELETE
Why /api/auth/ prefix for routes

Server Status ✅
Server is running on port: 5000
DB Connected

day04:

'register' - routes:
    // Step 1: Get data from request body
    // Step 2: Check if user already exists
    // Step 3: Hash password
    // Step 4: Save new user to DB
    // Step 5: Create JWT token
    // Step 6: Send token in cookie
    // Step 7: Send success response
    ⏳ Step 8: JWT token — coming next
    ⏳ Step 9: Welcome email — coming next

# try-catch:

        const register = async (req, res) => {
            try {
                // ALL your code here
                // get data
                // validate fields
                // check existing user
                // hash password
                // create user
                // send response
            } catch(error) {
                return res.status(500).json({
                    success: false,
                    message: 'Server error',
                    error: error.message
                })
            }
        }

'login'- routes
    // Step 1: Get email, password from req.body
    // Step 2: Validate missing fields
    // Step 3: Check user exists in DB
    // Step 4: Compare password with bcryptjs.compare()
    // Step 5: Send success or error response
    ⏳ Step 6: JWT token — coming next

# Concepts Learned Today:
    - bcryptjs.hash() — hashing password before saving
    - bcryptjs.compare() — comparing plain vs hashed password
    - Why same password gives different hash every time — random salt
    - How .compare() works internally — extracts salt from stored hash
    - SHA256 vs Bcrypt — deterministic vs random salt
    - try/catch — why and when catch executes
    - .select('-password') — when to use and when not to
    - .toObject() — converting mongoose document to plain JS object
    - delete object.property — removing sensitive data before response
    - return before res.json() — preventing double response error
    - Named exports — export { register, login }

